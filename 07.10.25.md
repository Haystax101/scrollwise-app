1. In onboarding, we ask the user to set a target streak to reach. But we then aren't properly tracking their streak (and for this reason, we stopped displaying streak in profile for the moment.). You should investigate our current implementation by exploring databaseOverview.sql to see our current database setup. We may have a dedicated streaks table, as well as a field of the profiles table dedicated to streaks so you should decide which is a better implementation for scalability and performance. You should then attempt to use that solution to get our streaks working properly, so that logging in each day (new day starts at midnight G.M.T) increases a user's streak by 1, and not logging in for a day (e.g. if they log in on the 27th but then don't log in in the following 24h bracket) causes the streak to reset to 1 (recall that streak should never be 0, always at least one). Create streaksPlan.md which outlines your proposed changes so i can review.
2. Once we've got streaks working, we need to work on notifications. Currently we aren't notifying users when events happen. Here's a list of events of which we should notify users: When someone adds you as a friend, when someone likes your content, when someone comments on your content, when someone saves your content, when your friend posts an insight, a daily notification reminding users to continue their streak. We must, of course, consider how we're going to handle sending notifications in the background (when the app is not running). You should consider this deeply - how do we send notifications when the app isn't running? We'll likely need to have some database table triggers tracking when these things happen, that i know, but i'm unsure about how notification sending works (predominantly on ios, though expo likely handles it for both ios and android right?) You should consider how big apps like instagram, snapchat etc. do it as we want it to be as real-time as possible. You should create a notificationsPlan.md file which outlines your considerations, implementation details and any questions / decisions for me so i can review.
3. We need to adjust onboarding (onboarding/) to reflect the new profile sections, as it's currently asking about certain things which are no longer displayed in the profile section. Review databaseOverview.sql to decide which data we're no longer displaying in NewProfile and suggest tables / fields which can be deleted. You should also make changes to the onboarding flow e.g removing / replacing components to ensure we keep it as succinct as possible. I'm thinking removing asking about where they work and what they do for work, and replacing with asking them to enter a tagline (we display this in newprofile, see databaseOverview for where it's stored). Create onboardingRestructuringPlan.md which outlines your proposed changes so I can review.
